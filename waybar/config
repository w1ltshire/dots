{
	"layer": "top",
	"position": "top",
	"height": 38,
	"spacing": 5,
	"modules-left": [
		"hyprland/workspaces"
	],
	"modules-right": [
		"cpu",
		"memory",
		"temperature",
		"pulseaudio",
    "custom/language",
	],
	"modules-center": [
    "clock"
	],
	"hyprland/workspaces": {
		"format": "{icon}",
		"format-active": " {icon} ",
		"on-click": "activate"
	},
  "custom/language": {
    "exec": "hyprctl devices -j | jq -r '.keyboards[] | select(.name == \"--varmilo\") | .active_keymap' | cut -c 1-3 | tr 'A-Z' 'a-z' | awk '{ print toupper($0) }'",
    "format": "<span color=\"#8fb8f2\">LNG</span>  {text}",
    "interval": 1
  },
  "clock": {
		"tooltip-format": "<big>{:%Y %B}</big>",
		"interval": 60,
		"format": "<span>-></span> {:%a %d %b %H:%M} <span>&lt;-</span>",
		"max-length": 25
	},
	"cpu": {
		"interval": "15",
		"format": "<span color=\"#f28fad\">CPU</span>  {usage}%",
		"max-length": 10
	},
	"memory": {
		"interval": 30,
		"format": "<span color=\"#f8bd92\">MEM</span> {}%",
		"max-length": 10
	},
	"temperature": {
		"hwmon-path": "/sys/class/hwmon/hwmon1/temp1_input",
		"critical-threshold": 80,
		"format-critical": "<span color=\"#f7a6a6\">CRIT</span> {temperatureC} °C",
		"format": "<span color=\"#FAE3B0\">TMP</span> {temperatureC} °C",
		"interval": 2
	},
	"pulseaudio": {
		"tooltip": false,
		"scroll-step": 5,
		"format": "{icon}  {volume}%",
		"format-muted": " MUT ",
		"on-click":"pactl set-sink-mute @DEFAULT_SINK@ toggle",
		"format-icons": {
			"default": ["<span color=\"#ABE9B3\">VOL</span>", "<span color=\"#ABE9B3\">VOL</span>", "<span color=\"#abe9b3\">VOL</span>"]
		}
	},
}
